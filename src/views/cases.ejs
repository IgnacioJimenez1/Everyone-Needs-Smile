<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/stylecases.css">
    <link rel="icon" href="images/LogoEVERYONE.png" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />

</head>

<body>
    <%- include('components/navbar') %>

    <main class="mainclass">
        <section class="hero">
            <h1>Construyamos un futuro mejor.</h1>
            <button class="buttonclass">Compartir un caso</button>
            <button class="buttonclass">Aportar en un caso</button>
        </section>
        <section id="cases" class="cases">
            <h2>Casos Publicados</h2>
            <% data.forEach(function(caso) { %>
            <div class="case">
                    <div class="case-info">
                        <h2>
                            <%= caso.title %>
                        </h2>
                        <p>
                            <%= caso.description %>
                        </p>
                        <div class="donation-info">
                            <button>Aportar</button>
                            <span>Prioridad</span>
                            <p>
                                <%= caso.description %>
                            </p>
                            <p><strong>Meta de Financiamiento:</strong>
                                <%= caso.fundinggoal %>
                            </p>
                            <p><strong>Recaudado:</strong>
                                <%= caso.amountraised %>
                            </p>
                            <p><strong>Fecha de Inicio:</strong>
                                <%= caso.startdate %>
                            </p>
                            <p><strong>Fecha de Fin:</strong>
                                <%= caso.enddate %>
                            </p>
                        </div>
                        </div>
                        <div class="cimage">
                            <img src="data:image/jpeg;base64,<%= caso.image.toString('base64') %>"
                                alt="Imagen del Caso">
                        </div>
                    </div>
                </div>
                    <% }); %>
        </section>
    </main>

    <footer>
        <%- include('components/footer') %>
      </footer>     
</body>

</html>
<li class="buttonclass">